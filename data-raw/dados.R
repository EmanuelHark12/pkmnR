normalize <- poke_type('normal')
normal <- poke_df(normalize)
usethis::use_data(normal, overwrite = TRUE)

fightingize <- poke_type('fighting')
fighting <- poke_df(fightingize)
usethis::use_data(fighting, overwrite = TRUE)

iceize <- poke_type('ice')
ice <- poke_df(iceize)
usethis::use_data(ice, overwrite = TRUE)

fireze <- poke_type('fire')
fire <- poke_df(fireze)
usethis::use_data(fire, overwrite = TRUE)

fairyize <- poke_type('fairy')
fairy <- poke_df(fairyize)
usethis::use_data(fairy, overwrite = TRUE)

darkize <- poke_type('dark')
dark <- poke_df(darkize)
usethis::use_data(dark, overwrite = TRUE)

bugize <- poke_type('bug')
bug <- poke_df(bugize)
usethis::use_data(bug, overwrite = TRUE)

rockize <- poke_type('rock')
rock <- poke_df(rockize)
usethis::use_data(rock, overwrite = TRUE)

groundize <- poke_type('ground')
ground <- poke_df(groundize)
usethis::use_data(ground, overwrite = TRUE)

ghostize <- poke_type('ghost')
ghost <- poke_df(ghostize)
usethis::use_data(ghost, overwrite = TRUE)

dragonize <- poke_type('dragon')
dragon <- poke_df(dragonize)
usethis::use_data(dragon, overwrite = TRUE)
